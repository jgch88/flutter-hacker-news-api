import 'package:flutter/material.dart';

class NewsDetail extends StatelessWidget {
  Widget build(context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail'),
      ),
      body: Text("I'm a details screen"),
    );
  }
}